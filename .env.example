# --- Traefik Configuration ---
# Domain name for routing traffic to the DB Gateway
TRAEFIK_DB_DOMAIN=db.localhost

# --- Docker Configuration ---
# The network name for all services to communicate
CHIMERA_NETWORK=db-forge-net

# --- DB Gateway Configuration ---
# The base image to use for spawning new database workers
DB_WORKER_IMAGE=db-worker-base:latest

# The directory on the host where persistent database volumes will be stored
# This ensures data sovereignty and survives container removal.
DB_DATA_PATH=./db-data

# Path to the file storing admin credentials (outside the container for persistence)
ADMIN_CREDS_PATH=./secrets/admin.json
