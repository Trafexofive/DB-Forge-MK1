[tool:pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
python_classes = Test*
addopts = 
    --verbose
    --tb=short
    --strict-markers
markers =
    integration: Integration tests that require running DB-Forge server
    unit: Unit tests that don't require external dependencies